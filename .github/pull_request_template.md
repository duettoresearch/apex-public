## What changed

<!-- One or two sentences. Link the issue if there is one. -->

## Leak gate

- [ ] `npm run leak-check` output pasted below, or the `ci` check is green
- [ ] No new file under `public/` contains legible names
- [ ] If `src/generated/` changed, the content diff was read, not just skimmed

```
<!-- paste the [leak-check] lines here, including the denylist tier line -->
```
